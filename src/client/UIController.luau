--!strict
-- State-driven UI. Phase 7 builds MainUI and subscribes Render() to
-- StateUpdate.

local UIController = {}

function UIController.Init(_deps)
end

function UIController.Render(_state)
end

return UIController
